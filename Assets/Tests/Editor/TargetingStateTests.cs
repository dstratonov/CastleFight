// Placeholder file kept so the generated editor csproj still resolves this path.
// The actual tests live in BoundsHelperTests.cs to avoid duplicate runtime
// definitions while Unity's generated project files lag behind filesystem edits.
